export default class Playlist {
  // constructor( MusicLibrary )

  // list: libraryListIndices[] - index of Song in Library.list
  // move( fromIndex, toIndex )
  // insert( atIndex ) - check for duplicates
  // delete( index )

  // active: index
  // skip(): active - loops around on the end of the playlist
  // prev(): active
  // private updateActive() - run on skip, prev, move, insert, delete to keep active consistant
}
